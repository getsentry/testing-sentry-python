[project]
name = "test-asyncpg"
version = "0"
requires-python = ">=3.12"

dependencies = [
    "asyncpg>=0.30.0",
    "ipdb>=0.13.13",
    "sentry-sdk[asyncpg]>=2.68.1",
]

[tool.uv.sources]
sentry-sdk = { path = "../../sentry-python", editable = true }
