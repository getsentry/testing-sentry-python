[project]
name = "test-lambda-function"
version = "0"
requires-python = ">=3.12"

dependencies = [
    "sentry-sdk>=2.68.1",
]

[tool.uv.sources]
sentry-sdk = { path = "../../sentry-python", editable = true }
