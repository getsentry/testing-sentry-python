[project]
name = "test-django-asgi"
version = "0"
requires-python = ">=3.12"

dependencies = [
    "django>=5.2",
    "ipdb>=0.13.13",
    "sentry-sdk[django]",
    "uvicorn>=0.34.0",
]

[tool.uv.sources]
sentry-sdk = { path = "../../sentry-python", editable = true }
