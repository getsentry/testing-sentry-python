[project]
name = "test-lambda-container"
version = "0"
requires-python = ">=3.11"

dependencies = [
    "boto3>=1.38.0",
    "sentry-sdk",
]

[tool.uv.sources]
sentry-sdk = { path = "../../sentry-python", editable = true }
