[project]
name = "test-starlette"
version = "0"
requires-python = ">=3.12"

dependencies = [
    "ipdb>=0.13.13",
    "starlette>=0.46.1",
    "uvicorn>=0.34.0",
    "sentry-sdk[starlette]",
]

[tool.uv.sources]
sentry-sdk = { path = "../../sentry-python", editable = true }
