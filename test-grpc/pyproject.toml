[project]
name = "test-grpc"
version = "0"
requires-python = ">=3.12"

dependencies = [
    "grpcio>=1.71.0",
    "grpcio-tools>=1.71.0",
    "ipdb>=0.13.13",
    "sentry-sdk[grpcio]",
]

[tool.uv.sources]
sentry-sdk = { path = "../../sentry-python", editable = true }
