[project]
name = "test-python312"
version = "0"
requires-python = ">=3.12"

dependencies = [
    "ipdb>=0.13.13",
    "sanic>=24.12.0",
    "sentry-sdk[sanic]",
]

[tool.uv.sources]
sentry-sdk = { path = "../../sentry-python", editable = true }
