[project]
name = "test-huey"
version = "0"
requires-python = ">=3.12"

dependencies = [
    "huey>=2.0",
    "ipdb>=0.13.13",
    "redis>=5.2.1",
    "sentry-sdk",
]

[tool.uv.sources]
sentry-sdk = { path = "../../sentry-python", editable = true }
