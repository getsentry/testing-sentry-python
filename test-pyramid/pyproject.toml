[project]
name = "test-pyramid"
version = "0"
requires-python = ">=3.12"

dependencies = [
    "ipdb>=0.13.13",
    "pyramid>=2.0",
    "sentry-sdk[pyramid]>=2.68.1",
    "waitress>=3.0",
]

[tool.uv.sources]
sentry-sdk = { path = "../../sentry-python", editable = true }
