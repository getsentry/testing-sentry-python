[project]
name = "test"
version = "0"
requires-python = ">=3.12"

dependencies = [
    "celery>=5.5.3",
    "ipdb>=0.13.13",
    "redis>=6.2.0",
    "sentry-sdk>=2.68.1",
]

[tool.uv.sources]
sentry-sdk = { path = "../../sentry-python", editable = true }
